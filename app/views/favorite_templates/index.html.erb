<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All favorites
    </h1>

    <a href="/favorites/new" class="btn btn-block btn-outline-secondary">
      Add a new favorite
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Recipes
        </th>

        <th>
          User
        </th>

        <th>
          Cuisine
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @favorites.each do |favorite| %>
      <tr>
        <!-- Display favorite.id start -->
        <td>
          <%= favorite.id %>
        </td>
        <!-- Display favorite.id end -->

        <!-- Display favorite.recipes_id start -->
        <td>
          <% if favorite.recipes.present? %>
            <a href="/recipes/<%= favorite.recipes.id %>">
              <%= favorite.recipes.user_id %>
            </a>
          <% end %>
        </td>
        <!-- Display favorite.recipes_id end -->

        <!-- Display favorite.user_id start -->
        <td>
          <% if favorite.user.present? %>
            <a href="/users/<%= favorite.user.id %>">
              <%= favorite.user.email %>
            </a>
          <% end %>
        </td>
        <!-- Display favorite.user_id end -->

        <!-- Display favorite.cuisine_id start -->
        <td>
          <% if favorite.cuisine.present? %>
            <a href="/cuisines/<%= favorite.cuisine.id %>">
              <%= favorite.cuisine.cuisine %>
            </a>
          <% end %>
        </td>
        <!-- Display favorite.cuisine_id end -->

        <!-- Display favorite.created_at start -->
        <td>
          <%= time_ago_in_words(favorite.created_at) %> ago
        </td>
        <!-- Display favorite.created_at end -->

        <!-- Display favorite.updated_at start -->
        <td>
          <%= time_ago_in_words(favorite.updated_at) %> ago
        </td>
        <!-- Display favorite.updated_at end -->

        <td>
          <a href="/favorites/<%= favorite.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @favorites, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
